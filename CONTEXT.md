# Universal AI Gateway

多协议双向转换的 AI 网关数据面。核心问题不是「怎么调通上游」，而是
**在协议之间搬运请求时，哪些语义会丢、丢了要不要让人知道**。下面的术语几乎
都是围绕这个问题长出来的。

## Language

### 协议与路径

**入站协议（Inbound Protocol）**：
客户端对网关说话用的线格式，例如 `openai.responses`、`dashscope.native`。
_Avoid_: 客户端协议、前端协议、API 格式

**出站 Provider（Outbound Provider）**：
网关对上游说话用的线格式。与入站协议是两个独立维度——同一个入站协议可以配任意
出站 Provider。
_Avoid_: 后端、上游、渠道、channel

**协议族（Protocol Family）**：
共用编解码基础设施与错误信封的一组入站协议，例如 OpenAI 族含 Responses、Chat、
Realtime 三个协议。优先级按族排而非按单协议排。
_Avoid_: 协议组、供应商

**路径（Route）**：
一对确定的 (入站协议, 出站 Provider)。网关的全部行为差异都挂在路径上。
_Avoid_: 通道、链路、映射

**同源快通道（Homogeneous Fast Path）**：
入站协议与出站 Provider 属于同一线格式的路径。可字节级透传，只改写鉴权，
不经 Canonical。
_Avoid_: 直连、passthrough 模式、透传通道

### 能力与处置

**能力（Capability）**：
一项可被请求、可能在转换中丢失的语义特性，例如「流式」「推理签名」
「服务端会话」。不是「模型支不支持」，而是「协议表达得出、且转换要负责搬运」
的东西。
_Avoid_: 特性、feature、功能点

**可表达性（Expressibility）**：
某个**入站协议**是否有字段能让客户端发出某项能力。这是协议的属性，不是路径的
属性——OpenAI Chat 的客户端没有字段可以发出推理签名，与任何一条路径无关。
_Avoid_: 支持度、兼容性

**处置（Disposition）**：
一条路径对一项能力的裁决，五取一：`PASSTHROUGH` / `EMULATE` / `DEGRADE` /
`REJECT` / `N/A`。
_Avoid_: 状态、结果、策略

**降级（Degrade）**：
请求仍然成功，但该能力的部分语义被丢弃。区别于拒绝——后者请求根本不发出去。
_Avoid_: 兼容处理、弱化、fallback

**模拟（Emulate）**：
上游不提供该能力，由网关自行实现。客户端拿到的能力是完整的，但带着网关侧的
可用性边界。
_Avoid_: 兜底、polyfill、代理实现

**不适用（N/A）**：
该能力在这条路径上不可达——入站协议表达不出来，客户端连发都发不出来。
不是拒绝，也不计入保留度分母。
_Avoid_: 不支持、无关

**降级矩阵（Degradation Matrix）**：
全部 (路径 × 能力) 处置的集合。它是网关唯一的权威声明来源，未登记的组合一律
拒绝。
_Avoid_: 能力表、兼容性矩阵、支持列表

### 度量与状态

**保留度（Preservation）**：
一条路径保住了多少原生能力，分母只算入站协议可表达的能力。分**设计目标**与
**当前可用**两列——后者受实现状态与默认配置影响。
_Avoid_: 兼容度、评分、覆盖率

**转正（Promotion）**：
把一条路径从 `PLANNED` 改为已实现。门槛是端到端 fixture 通过，不是有人认为
它写完了。
_Avoid_: 上线、启用、完成

**PLANNED**：
已在矩阵中登记处置、但尚未实现的路径。运行时对它的请求明确报错，不静默失败。
_Avoid_: TODO、待办、未完成

### 用量与错误

**可信等级（Fidelity）**：
一份用量数据的来源可靠程度：`authoritative`（上游返回，可计费）/
`estimated`（本地估算）/ `unavailable`（不可知）。零值非法。
_Avoid_: 精度、准确度、来源

**可重试（Retryable）**：
「换一个凭据或 Provider 重试可能成功」。**不是**「上游是否临时故障」——
配额耗尽对当前凭据是确定性失败，但对池里另一个凭据可能成功，因此可重试。
_Avoid_: 临时错误、可恢复

**首字节边界（First-Byte Boundary）**：
流式响应发出第一个字节的时刻。之前失败可以 failover，之后一律不重试——
重试会让客户端收到重复内容。
_Avoid_: 流式起点、响应开始
